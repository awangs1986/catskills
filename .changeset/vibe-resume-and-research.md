---
"mattpocock-skills": minor
---

`vibe`: when invoked with no argument and in-flight work exists, hand back a where-you-were block (branch, tickets with status, last commits, newest refocus brief, next ticket on the frontier) before asking the lane question; route through `/setup-feedback-loops` when `docs/agents/feedback-loops.md` is missing. The workflow handbook brings `research` back into the kit for library and API questions, adds `setup-feedback-loops` as the second setup step, and threads `verify` and `security-review` through the Build and Review lanes.
