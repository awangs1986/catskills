---
"mattpocock-skills": patch
---

`implement` now claims its ticket at the start (`Status: in-progress` locally; assign and comment on GitHub/GitLab) and closes it at the end (`Status: done` plus the commit sha and its Checks run block under `## Comments`; close or label on a real tracker), ticking the acceptance criteria it met. The local-markdown tracker doc documents the three-state implementation ticket lifecycle and defines the frontier in terms of it, so `/vibe`'s where-you-were block and "next ticket on the frontier" have something real to read. The `vibe` handbook adds docs-drift reconciliation to the Tidy lane and a section on `retro` as the loop that closes on the environment rather than the product.
